# backend/attendance/

Stub Django app. `models.py`/`views.py` contain only the default Django boilerplate — no model
classes or view functions defined. No `urls.py`, and not listed in `INSTALLED_APPS`
(`backend/config/settings.py`).

Reserved for the Attendance module (Weekly Attendance, Attendance Review, Attendance Reports —
see root `README.md` § Module Structure and `docs/03_Solution/modules/attendance/README.md`).
