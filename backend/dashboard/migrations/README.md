# backend/dashboard/migrations/

Django migrations for the `dashboard` app. Currently only `__init__.py` — no migrations exist
because `models.py` has no model classes (`dashboard` has only a view, see
`backend/dashboard/README.md`).
