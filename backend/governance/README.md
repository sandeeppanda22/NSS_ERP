# backend/governance/

Stub Django app. `models.py`/`views.py` contain only the default Django boilerplate — no model
classes or view functions defined. No `urls.py`, and not listed in `INSTALLED_APPS`
(`backend/config/settings.py`). Not to be confused with `docs/00_Project_Governance/`, which
governs the project itself rather than this ERP module.

Reserved for the Governance module (General Body, Governing Body, Advisory Board, Committees,
Position Assignment — see root `README.md` § Module Structure and
`docs/03_Solution/modules/governance/README.md`).
