# backend/membership/migrations/

Django migrations for the `membership` app.

- `0001_initial.py` — creates `MembershipType`, `MembershipStatus`, `SanghaSevi` (see
  `backend/membership/README.md`).
