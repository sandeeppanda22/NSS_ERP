# backend/static/

Project-level static assets (`STATIC_URL = 'static/'` in `backend/config/settings.py`; no
`STATICFILES_DIRS`/`STATIC_ROOT` configured).

- `css/` — `app.css`
- `js/` — `app.js` (empty placeholder)
