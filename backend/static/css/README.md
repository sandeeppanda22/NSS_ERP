# backend/static/css/

- `app.css` — NSS color theme: CSS custom properties (`--nss-primary`, `--nss-secondary`,
  `--nss-sidebar`, `--nss-navbar`, `--nss-background`) plus styles for `.nss-navbar`,
  `.nss-sidebar` (incl. `.nav-link` hover state), and `.login-body`. Loaded alongside the
  Bootstrap 5.3.3 CDN in `templates/base/base.html`.
