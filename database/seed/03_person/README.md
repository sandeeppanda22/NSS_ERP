# database/seed/03_person/

> **Superseded prototype** — seeds tables (`gender_master`, etc.) that don't exist under the
> new `master_data` pattern implemented in `database/seed/01_foundation/`. See
> `database/README.md` "Superseded Artifacts."

- `01_person_master_tables.sql` — seed rows for `gender_master` (MALE, FEMALE, OTHER),
  `marital_status_master` (UNMARRIED, MARRIED, WIDOWED, DIVORCED, SEPARATED), and
  `address_type_master` (PERMANENT, CURRENT, OFFICIAL).
