# NSS ERP Foundation Module

Status: NOT STARTED as a standalone Solution doc set — Foundation's actual person/organization
design lives in the sibling `docs/03_Solution/modules/person/` and
`docs/03_Solution/modules/organization/` folders, and `backend/foundation/` is the
corresponding Django app (`OrganizationType`, `Organization`, `Address`, `Person` models; see
`backend/foundation/README.md`).

This folder is reserved for any Foundation-module design content that doesn't belong under
`person/` or `organization/` specifically (e.g. master data, global location management — see
`README.md` § Module Structure). No content has been written yet.
